{
  "name": "nemv",
  "version": "0.0.1",
  "scripts": {
    "dev": "cross-env NODE_ENV=development PORT=3000 node ./be/bin/www",
    "serve": "cd fe && yarn serve",
    "pr": "cd fe && yarn && yarn build && yarn serve && cd ../be && yarn && cd .. && cross-env NODE_ENV=production PORT=80 node ./be/bin/www"
  },
  "dependencies": {
    "cross-env": "^5.2.0"
  }
}
